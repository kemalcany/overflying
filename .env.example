# Planet Monorepo - Environment Configuration Template
# Copy this file to create environment-specific configs:
#   .env.development (Docker - default)
#   .env.local (Local Postgres)
#   .env.staging (GCP Staging)
#   .env.production (GCP Production)

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=postgresql+psycopg2://user:password@host:port/dbname

# Examples:
# Development (Docker):  postgresql+psycopg2://postgres:postgres@localhost:5432/planet
# Local:                 postgresql+psycopg2://username@/planet?host=/tmp
# Staging (Cloud SQL):   postgresql+psycopg2://postgres:PASSWORD@localhost:5433/planet
# Production (Cloud SQL): postgresql+psycopg2://postgres:PASSWORD@localhost:5434/planet

# =============================================================================
# Message Queue
# =============================================================================
NATS_URL=nats://localhost:4222

# Examples:
# Local:      nats://localhost:4222
# Staging:    nats://staging-nats.example.com:4222
# Production: nats://prod-nats.example.com:4222

# =============================================================================
# Common Settings
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Environment values: development, local, staging, production
# Log levels: DEBUG, INFO, WARNING, ERROR, CRITICAL

# =============================================================================
# API Configuration
# =============================================================================
HOST=0.0.0.0
PORT=8000

# =============================================================================
# Worker Configuration
# =============================================================================
POLL_INTERVAL=5
GPU_SIMULATION=true

# GPU_SIMULATION: true for development/local, false for staging/production
# POLL_INTERVAL: seconds between job checks

# =============================================================================
# Globe (Frontend) Configuration
# =============================================================================
# NEXT_PUBLIC_CESIUM_TOKEN=your-cesium-token-here
# NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# App-Specific Overrides
# =============================================================================
# For app-specific settings that differ by environment, create:
#   apps/api/.env.staging.override
#   apps/api/.env.production.override
#   apps/worker/.env.staging.override
#   apps/worker/.env.production.override
#   apps/globe/.env.staging.override
#   apps/globe/.env.production.override
#
# These files will be loaded AFTER the root .env file and can override any value.
