# Database connection
# Docker Postgres (recommended - shared across repos)
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/planet

# Local Postgres (alternative - uncomment to use)
DATABASE_URL=postgresql+psycopg2://kemal@/planet?host=/tmp

# API settings
HOST=0.0.0.0
PORT=8000
